services:
  mcp-atlassian-multi-user:
    image: ghcr.io/${REGISTRY_ID}/${PROJECT_NAME}:${VERSION} # Primary image name for reference
    build:
      context: .
      dockerfile: Dockerfile
      platforms:
        - linux/amd64
      tags:
        # GHCR tags
        - "ghcr.io/${REGISTRY_ID}/${PROJECT_NAME}:${VERSION}"
        - "ghcr.io/${REGISTRY_ID}/${PROJECT_NAME}:latest"
        # Docker Hub tags
        - "${REGISTRY_ID}/${PROJECT_NAME}:${VERSION}"
        - "${REGISTRY_ID}/${PROJECT_NAME}:latest"
